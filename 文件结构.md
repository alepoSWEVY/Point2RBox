• 这是一个基于 MMRotate/MMDetection 的“点标注 → 旋转框检测”研究仓库。核心是 Point2RBox-v3，但也保留了 Point2RBox v1/v2、H2RBox、Wholly-WOOD 等对比方法。

  整体运行链路是：

  配置文件 configs/
        ↓
  tools/train.py 或 tools/test.py
        ↓
  数据集与数据变换 mmrotate/datasets/
        ↓
  检测器 point2rbox_v3.py
        ↓
  检测头 point2rbox_v3_head.py
        ↓
  伪标签、SAM/分水岭、边缘约束、损失函数
        ↓
  评测 mmrotate/evaluation/

  ## 1. 根目录文件

  - README.md：项目总说明、论文结果、训练测试命令和模型下载地址。
  - environment.md：环境安装说明，重点规定 PyTorch、CUDA、MMCV、MMDetection 和 NumPy 版本。
  - data.md：各数据集的下载、目录结构、切图和配置路径。
  - train.sh：交互式训练启动器；选择 Conda 环境、GPU、配置及训练参数。
  - test.sh：交互式测试启动器；选择配置、权重和输出方式。
  - setup.py：把当前源码安装成 mmrotate Python 包。
  - setup.cfg：isort、YAPF、codespell 等代码格式化规则。
  - requirements.txt：聚合各类依赖清单。
  - requirements/：细分运行、测试、构建、文档和可选依赖。
  - mobile_sam.pt：MobileSAM 模型权重，用点提示生成目标分割掩码。
  - pyvenv.cfg：某个 Python 3.10 虚拟环境遗留的元数据；不是项目算法文件，而且和文档推荐的 Python 3.12 不一致。
  - .gitignore：Git 忽略规则。

  ## 2. Point2RBox-v3 核心代码

  最值得先看的三个文件是：

  - mmrotate/models/detectors/point2rbox_v3.py：整个 v3 方法的总控制器。
  - mmrotate/models/dense_heads/point2rbox_v3_head.py：预测头、标签分配和各项损失的调度。
  - mmrotate/models/losses/point2rbox_v2_loss.py：虽然文件名是 v2，但 v3 也复用了其中的 SAM、分水岭、边缘和一致性损失。

  ### point2rbox_v3.py

  主要负责：

  - 将点监督标注转成初始旋转框伪标签。
  - 为同一张图片创建旋转、翻转或缩放后的第二视图。
  - 维护两个视图之间的目标对应关系。
  - 调用 TED 网络提取图像边缘。
  - 在训练后半程将预测目标剪下来，再随机旋转、缩放并复制粘贴。
  - 前期使用几何/SAM伪标签，后期切换到模型自己预测的伪标签。
  - 最后调用 Point2RBoxV3Head 计算损失。

  其中 bids 是一个关键内部字段：

  [batch_id, 是否合成目标, 属于哪个视图, 原目标ID]

  它用于匹配原图、增强图和复制粘贴目标。

  ### point2rbox_v3_head.py

  负责：

  - FPN 多尺度特征上的分类、宽高和角度预测。
  - 从点位置读取候选旋转框。
  - 在不同 FPN 层候选框之间选择伪标签。
  - Progressive Label Assignment：训练过程中逐步用模型输出替换初始伪框。
  - 调用分类、框回归、重叠、分水岭、边缘和双视图一致性损失。
  - pseudo_generator=True 时，把训练集预测结果导出为第二阶段伪标注。

  ### 损失相关文件

  - point2rbox_v2_loss.py：核心弱监督损失集合，包括 GaussianOverlap、Voronoi/Watershed、SAM掩码、EdgeLoss 和一致性损失。
  - utils.py：SAM候选掩码过滤；计算矩形度、圆度、颜色一致性、中心对齐等指标。
  - vis.py：保存分水岭、SAM筛选和损失计算的调试图。
  - gaussian_dist_loss.py、gaussian_dist_loss_v1.py：把旋转框表示成二维高斯分布并计算 GWD/KLD 等距离。
  - rotated_iou_loss.py：旋转框 IoU 损失。
  - convex_giou_loss.py：凸多边形 GIoU。
  - kf_iou_loss.py：基于卡尔曼滤波思想的旋转框损失。
  - smooth_focal_loss.py：平滑版本 Focal Loss。
  - spatial_border_loss.py：约束预测点集靠近目标边界。
  - h2rbox_consistency_loss.py、h2rbox_v2_consistency_loss.py：H2RBox 系列旋转增强一致性损失。

  ## 3. Point2RBox-v3 配置

  目录：configs/point2rbox_v3

  ### 主训练配置

  - point2rbox_v3-1x-dotav1-0.py：DOTA v1.0，15 类。
  - point2rbox_v3-1x-dotav1-5.py：DOTA v1.5，16 类。
  - point2rbox_v3-1x-dior.py：DIOR，20 类。
  - point2rbox_v3-1x-star.py：STAR，48 类。

  它们定义：

  - ResNet-50 + FPN。
  - Point2RBoxV3 检测器和 Point2RBoxV3Head。
  - 数据增强、批大小、学习率和训练轮数。
  - 第几轮启用边缘损失、复制粘贴和预测伪标签。
  - 哪些类别近似正方形、圆形或需要额外尺寸修正。

  ### SAM基础配置

  - _base_sam-dotav1-0.py
  - _base_sam-dotav1-5.py
  - _base_sam-dior.py
  - _base_sam-star.py

  它们为各数据集定义 SAM 掩码选择规则。例如不同类别对矩形度、圆度、长宽比、颜色一致性和中心对齐赋予不同权重。

  ### 两阶段训练配置

  - point2rbox_v3-pseudo-generator-*.py：载入第一阶段模型，在训练集上生成旋转框伪标注 JSON。
  - rotated-fcos-1x-*-using-pseudo.py：用生成的伪框训练一个普通全监督 Rotated FCOS。

  所以仓库支持两种模式：

  端到端：点标注 → Point2RBox-v3 → 检测结果

  两阶段：点标注 → v3生成伪框 → Rotated FCOS重新训练

  ## 4. 其他配置目录

  ### configs/_base_

  - default_runtime.py：日志、检查点保存、可视化、随机种子和默认作用域。
  - schedules/schedule_1x.py：12 epoch。
  - schedule_3x.py：36 epoch。
  - schedule_6x.py：72 epoch。
  - schedule_40e.py：40 epoch。
  - datasets/*.py：每个数据集的数据路径、类别、读取流程、DataLoader 和评测器。

  数据集配置文件与名称基本一一对应：

  - dota.py、dotav15.py、dotav2.py
  - dota_ms.py：DOTA 多尺度切图。
  - dota_qbox.py：四点多边形标注形式。
  - dota_coco.py：COCO JSON 形式。
  - dior.py、star.py、fair.py、hrsc.py
  - ssdd.py、hrsid.py、srsdd.py、rsdd.py
  - rsar.py、sardet100k.py
  - sku110k.py、ocdpcb.py、diatom.py

  ### configs/point2rbox_v2

  Point2RBox-v2 的复现实验。DOTA、DIOR、FAIR、HRSC、SKU110K 通常各有三类配置：

  - point2rbox_v2-*.py：点监督模型训练。
  - point2rbox_v2-pseudo-generator-*.py：生成伪框。
  - rotated-fcos-*-using-pseudo.py：用伪框重新训练 Rotated FCOS。

  ### configs/point2rbox

  第一代 Point2RBox，使用 YOLOF 架构：

  - point2rbox-yolof-dota.py、dotav15.py、dotav2.py
  - point2rbox-yolof-dior.py、fair.py、hrsc.py
  - 名称带 setrc 的配置会额外处理近似圆形/方形类别。
  - metafile.yml：模型、配置和论文结果的元数据。

  ### configs/h2rbox 与 configs/h2rbox_v2

  水平框监督旋转检测的对比方法：

  - H2RBox：主要使用旋转增强的一致性学习。
  - H2RBox-v2：进一步支持 S2ANet、ReDet、多尺度和随机旋转。
  - 文件名中的 1x/3x/6x 表示训练长度。
  - le90 表示角度规范在约 90° 周期内表示。
  - r50_fpn 表示 ResNet-50 + FPN。
  - metafile.yml 和 README.md 记录模型信息和结果。

  ### configs/whollywood

  Wholly-WOOD 的实验配置：

  - whollywood-*-p2r-pseudo.py：点到旋转框伪标签。
  - whollywood-*-p2h-pseudo.py：点到水平框伪标签。
  - using-pseudo.py：使用伪标签进行后续训练。
  - ms_rr：多尺度加随机旋转。

  ## 5. 数据集代码

  目录：mmrotate/datasets/

  - dota.py：DOTA v1.0/v1.5/v2 数据读取。
  - dior.py：DIOR。
  - fair.py：FAIR1M。
  - hrsc.py：HRSC船舶数据集。
  - star.py：STAR。
  - rsar.py：RSAR。
  - sardet100k.py：SARDet-100K。
  - sku110k.py：SKU-110K密集商品检测。
  - ocdpcb.py：PCB缺陷数据集。
  - diatom.py：硅藻数据集。
  - __init__.py：导入并注册上述数据集，让配置中的字符串名称可以找到对应类。

  ### datasets/transforms

  - loading.py：从内存数组中加载图像块。
  - transforms.py：
      - ConvertBoxType：水平框、旋转框、四边形互转。
      - RBox2Point：将旋转框退化为点监督。
      - RBox2PointWithNoise：为点坐标加入噪声。
      - ConvertWeakSupervision：按比例生成点框/水平框弱监督。
      - Rotate、RandomRotate、RandomChoiceRotate：旋转增强。
      - ClampBox：把框限制在图像范围内。
      - ConvertMask2BoxType：转换掩码对应的框类型。

  - __init__.py：注册这些变换。

  ## 6. 旋转框数据结构

  目录：mmrotate/structures/bbox/

  - rotated_boxes.py：五参数旋转框 (cx, cy, w, h, angle)，支持旋转、缩放、翻转和裁剪。
  - quadri_boxes.py：八参数四边形框。
  - box_converters.py：hbox/rbox/qbox 相互转换。
  - bbox_overlaps.py：旋转框 IoU 和重叠度计算。
  - transforms.py：角度归一化、距离解码、框与高斯分布互转。
  - __init__.py：统一导出上述接口。

  ## 7. 模型代码

  ### models/detectors

  这些文件是完整检测器，负责串联 backbone、neck 和 head：

  - point2rbox_v3.py、point2rbox_v2.py、point2rbox_yolof.py
  - h2rbox.py、h2rbox_v2.py、h2rbox_v2p.py
  - h2rbox_v2_s2anet.py、h2rbox_v2_redet.py
  - whollywood_p2r.py、whollywood_p2h.py
  - refine_single_stage.py：支持多阶段逐步精修的单阶段检测器基类。
  - __init__.py：注册检测器。

  ### models/dense_heads

  检测头负责从特征图产生类别、框和角度：

  - point2rbox_v3_head.py、point2rbox_v2_head.py、point2rbox_yolof_head.py
  - rotated_fcos_head.py、rotated_fcos_bf_head.py
  - rotated_retina_head.py、rotated_atss_head.py
  - rotated_reppoints_head.py、oriented_reppoints_head.py
  - rotated_rtmdet_head.py
  - h2rbox_head.py、h2rbox_v2_head.py、h2rbox_v2p_head.py
  - h2rbox_v2_s2a_head.py
  - whollywood_p2r_head.py、whollywood_p2h_head.py
  - sam_reppoints_head.py：带空间对齐模块的 RepPoints。
  - s2a_head.py、r3_head.py、cfa_head.py：逐步精修类旋转检测头。
  - oriented_rpn_head.py：旋转候选区域生成。
  - angle_branch_retina_head.py：给 RetinaNet 增加独立角度分支。
  - __init__.py：注册所有检测头。

  ### Backbone、Neck 和层

  - backbones/re_resnet.py：旋转等变 ResNet。
  - necks/re_fpn.py：与等变特征配套的 FPN。
  - layers/align.py：AlignConv、可变形卷积对齐和特征精修模块。
  - utils/enn.py：基于 e2cnn 的旋转等变卷积构造工具。
  - utils/orconv.py：方向响应卷积及方向池化。
  - utils/ripool.py：旋转不变池化。
  - utils/misc.py：点集中心、层级特征整理等辅助函数。

  ### ROI相关

  - roi_heads/gv_ratio_roi_head.py：Gliding Vertex ROI Head。
  - bbox_heads/gv_bbox_head.py：预测顶点偏移和长宽比例。
  - convfc_rbbox_head.py：通用旋转框全连接头。
  - h2rbox_v2_convfc_bbox_head.py：H2RBox-v2 两阶段框头。
  - roi_extractors/rotate_single_level_roi_extractor.py：旋转 RoIAlign 特征提取。

  ## 8. 标签分配、编码和合成

  ### task_modules/assigners

  - point2rbox_assigner.py：Point2RBox 点与候选预测之间的匹配。
  - rotated_atss_assigner.py：旋转版 ATSS 动态分配。
  - sas_assigner.py：形状自适应分配。
  - convex_assigner.py、max_convex_iou_assigner.py：点集/凸多边形匹配。
  - rotate_iou2d_calculator.py：旋转框、水平框和四边形之间的 IoU 计算包装。

  ### task_modules/coders

  - angle_coder.py：CSL、PSC 和伪角度编码。
  - distance_angle_point_coder.py：FCOS点到旋转框的距离+角度编码。
  - distance_boundary_free_point_coder.py：无边界周期的点框编码。
  - delta_xywht_rbbox_coder.py、delta_xywht_hbbox_coder.py：带角度周期编码。
  - delta_xywh_psc_rbbox_coder.py、delta_xywh_psc_hbbox_coder.py：PSC角度版本。
  - delta_xywh_qbbox_coder.py：四边形框编码。
  - delta_midpointoffset_rbbox_coder.py：中点偏移表示。
  - gliding_vertex_coder.py：Gliding Vertex 参数编码。

  ### 其他

  - prior_generators/anchor_generator.py：伪旋转 Anchor 生成器。
  - synthesis_generators/point2rbox_generator.py：第一代 Point2RBox 的基础纹理载入、旋转合成和复制粘贴。

  ## 9. 评测与可视化

  ### mmrotate/evaluation

  - functional/mean_ap.py：旋转框 TP/FP、AP 和 mAP 计算。
  - metrics/dota_metric.py：DOTA格式结果合并、评测和提交文件生成。
  - metrics/fair_metric.py：FAIR1M评测与提交格式。
  - metrics/rotated_coco_metric.py：旋转框 COCO AP。
  - 各级 __init__.py：统一导出评测接口。

  ### mmrotate/visualization

  - local_visualizer.py：在图像上绘制旋转框、类别和分数。
  - palette.py：类别颜色表。
  - __init__.py：导出可视化器。

  ## 10. 工具脚本

  目录：tools/

  - train.py：正式训练入口；读取配置、注册模块、创建 MMEngine Runner。
  - test.py：测试、评估、导出结果和可视化入口。
  - dist_train.sh、dist_test.sh：单机多 GPU 分布式训练/测试。
  - slurm_train.sh、slurm_test.sh：Slurm 集群版本。
  - data_prepare.zip：数据预处理脚本压缩包；data.md 要求先解压。

  ### analysis_tools

  - analyze_logs.py：分析训练日志、画 loss/mAP 曲线。
  - benchmark.py：测速。
  - browse_dataset.py：检查数据读取和标注显示。
  - confusion_matrix.py：生成混淆矩阵。
  - get_flops.py：计算 FLOPs 和参数量。

  ### misc

  - print_config.py：展开配置继承后，打印最终完整配置。

  ### model_converters

  - publish_model.py：清理训练权重并生成适合发布的 checkpoint。

  ### deployment

  - mmrotate2torchserve.py：生成 TorchServe .mar 包。
  - mmrotate_handler.py：TorchServe 请求预处理、推理和结果后处理。

  ## 11. 推理和大图切片

  - mmrotate/apis/inference.py：把大幅遥感图像切成小块推理，再合并结果。
  - mmrotate/utils/patch/split.py：计算多尺度滑动窗口。
  - mmrotate/utils/patch/merge_results.py：把不同图块检测框映射回原图并做 NMS。
  - mmrotate/utils/misc.py：通用小工具。
  - mmrotate/utils/setup_env.py：注册模块并初始化 MMRotate作用域。
  - mmrotate/utils/collect_env.py：收集 PyTorch、CUDA、MMCV 等环境信息。

  ## 12. 其他资源

  - third_parties/ted/ted.py：轻量级 TED 边缘检测网络。
  - third_parties/ted/ted.pth：TED预训练权重。
  - basic_patterns/<dataset>/*.png：Point2RBox早期版本用于合成目标的类别基础纹理。
  - basic_patterns/<dataset>/properties.txt：各类别属性，例如是否近似圆形或方形。
  - figures/*.png：README和论文中的流程图、消融实验及结果图。
  - resources/*.png：各方法介绍页使用的缩略图。
  - poster/*.pptx/.pdf/.png：ICLR 2026 海报的源文件和导出版本。

  ## 13. 各种 __init__.py、registry.py 和版本文件

  - 所有 __init__.py：将类导入包命名空间，同时触发 MMEngine 注册器注册。看似没算法，但不能随意删除。
  - mmrotate/registry.py：定义模型、数据集、评测器、变换等注册表。
  - mmrotate/version.py：包版本号。
  - mmrotate/__init__.py：检查依赖版本并导出版本信息。
  - mmrotate/testing/：测试辅助数据和随机框生成工具，主要供单元测试使用。

  建议阅读顺序：

  1. configs/point2rbox_v3/point2rbox_v3-1x-dotav1-0.py
  2. mmrotate/models/detectors/point2rbox_v3.py
  3. mmrotate/models/dense_heads/point2rbox_v3_head.py
  4. mmrotate/models/losses/point2rbox_v2_loss.py
  5. mmrotate/datasets/transforms/transforms.py
  6. mmrotate/evaluation/metrics/dota_metric.py

  这六个文件基本能串起“数据如何变成点标注、伪框如何产生、模型如何训练、结果如何评测”的完整流程。
